[project]
name = "adrealytywidget-v2-0"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cachetools>=7.1.7",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "uvicorn>=0.52.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
